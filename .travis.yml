language: c
compiler: 
  - gcc
  - clang
script: ./autogen.sh && ./configure && make && make check
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y libgmp-dev libtcmalloc-minimal0 check octave3.2-headers
